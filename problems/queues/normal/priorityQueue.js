'use strict';
// A priority queue is a queue where values are also assigned a priority
// Values with higher priority are dequeued before values with lower priority
// Note that a "higher priority" means a LOWER numeric value,
//   e.g. item with priority 1 is higher priority than item with priority 5

var PriorityQueue = function(){

};

PriorityQueue.prototype.size = function(){

};

PriorityQueue.prototype.enqueue = function (value, priority){

};

PriorityQueue.prototype.dequeue = function(){

};

module.exports = PriorityQueue;
