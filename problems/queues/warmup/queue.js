/**
* Try to not use the native JavaScript methods push and pop
**/

var Queue = function() {
  // Your code here!
};


// enqueue will add an element to the queue
Queue.prototype.enqueue = function(value) {

};

// dequeue will remove an element from the queue and return that element
Queue.prototype.dequeue = function() {

};

// size will return the size of the queue
Queue.prototype.size = function() {

};

module.exports = Queue;
