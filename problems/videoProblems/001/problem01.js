'use strict';
/*
Write me a function that takes a size and returns a string of alternating '1s' and '0s'.

The size will always be positive and will only use whole numbers.

Remmember, run the tests by pointing at the problem set directory:
`npm test {path to this directory}`
*/

function stringy(size) {
  // your code here
}

module.exports = stringy;

