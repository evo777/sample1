/*
Convert a list of fractions into fractions with the same denominator.
The denominator should be minimized, i.e. the least common denominator.
*/

function convertToLCD(fractions) {
  // your code
}

module.exports = convertToLCD;