'use strict';
/*
Exactly the same setup as the previous problem, but use recursion if you didn't do that last time,
or avoid it now if you did.
*/

function getMiddle(str){
  //Your code here.
}

module.exports = getMiddle;
