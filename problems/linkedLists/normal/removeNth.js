'use strict';
/**
 * Write a function that takes a linked list and removes the nth element (0 indexed) from the list
 *
 * Linked list will be of the format:
 *
 * var linkedList = {
 *   value: 0,
 *   next: ...
 * };
 *
 */

var removeNth = function(linkedList, n) {
  // your code here
};

module.exports = removeNth;
