/**

* Write a function that when given a sorted matrix and a number, 
* return the position of that number [row, col] in logarithmic O(log(n)) time.
* The matrix is sorted by row and by column

* example:
* var matrix = [
*   [2, 4, 6, 7],
*   [5, 7, 8, 11],
*   [9, 10, 15, 18],
*   [11, 16, 20, 25],
* ]
*
* searchSortedMatrix(matrix, 15); // returns [2, 2]; 
* searchSortedMatrix(matrix, 15); // returns [2, 2];
* 
*
*/

var searchSortedMatrix = function(matrix, number){

};

module.exports ={
  searchSortedMatrix: searchSortedMatrix,
};