/*
 * Write a function that accepts a 2-dimensional array (that is, an array containing many
 * same-length arrays), and returns a 1-dimensional array with every value found, but in 
 * spiral order from the upper left in to the center.
 *
 * example:

    spiralPrint([
      [1,2,3],
      [4,5,6],
      [7,8,9]
    ]);

    returns [1, 2, 3, 6, 9, 8, 7, 4, 5]
 */

var spiralPrint = function(matrix){
 // your code here
};

module.exports ={
  spiralPrint: spiralPrint,
};
